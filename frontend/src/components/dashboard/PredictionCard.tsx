import GlassCard from "../common/GlassCard"; import {ClassBadge,ConfidenceBadge} from "../common/Badges"; import type {FireEvent} from "../../types/fire"; import {Link} from "react-router-dom";
export default function PredictionCard({event}:{event:FireEvent}){return <GlassCard className="prediction"><div className="pred-top"><span>FIRE EVENT · DEMO</span><span>{event.eventId}</span></div><div className="pred-title"><div><small>SOURCE CLASSIFICATION</small><h2><ClassBadge value={event.prediction.predictedClass}/></h2></div><div className="confidence"><strong>{(event.prediction.confidence*100).toFixed(1)}%</strong><ConfidenceBadge value={event.prediction.confidence}/></div></div><div className="pred-meta"><span>{event.location}</span><span>{event.activeLFs} active signals</span><span>{event.agreeingLFs} agreeing</span><span>{event.disagreeingLFs} disagreeing</span></div><Link className="text-link" to={`/events/${event.eventId}`}>Inspect evidence →</Link></GlassCard>}