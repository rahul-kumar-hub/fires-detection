import {ResponsiveContainer,LineChart,Line,XAxis,YAxis,Tooltip,BarChart,Bar,PieChart,Pie,Cell} from "recharts"; import type {AnalyticsData} from "../../types/api";
export function DistributionChart({data}:{data:AnalyticsData}){const rows=Object.entries(data.distribution).map(([name,value])=>({name,value}));return <ResponsiveContainer width="100%" height={270}><PieChart><Pie data={rows} dataKey="value" nameKey="name" innerRadius={72} outerRadius={105} paddingAngle={3}>{rows.map((_,i)=><Cell key={i} fill={`hsl(${12+i*48} 78% ${58-i*4}%)`}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer>}
export function TimelineChart({data}:{data:AnalyticsData}){return <ResponsiveContainer width="100%" height={270}><LineChart data={data.timeline}><XAxis dataKey="date"/><YAxis/><Tooltip/><Line type="monotone" dataKey="events" strokeWidth={3} dot={false}/></LineChart></ResponsiveContainer>}
export function ConfidenceChart({data}:{data:AnalyticsData}){return <ResponsiveContainer width="100%" height={270}><BarChart data={data.confidence}><XAxis dataKey="band"/><YAxis/><Tooltip/><Bar dataKey="count" radius={[10,10,0,0]}/></BarChart></ResponsiveContainer>}