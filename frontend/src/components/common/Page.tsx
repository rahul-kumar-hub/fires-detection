import {ReactNode} from "react"; import {motion} from "framer-motion"; export default function Page({title,subtitle,children,wide=false}:{title:string;subtitle?:string;children:ReactNode;wide?:boolean}){return <main className={`page ${wide?"wide":""}`}><motion.div initial={{opacity:0,y:14}} animate={{opacity:1,y:0}} transition={{duration:.45}} className="page-head"><div><div className="eyebrow">FIRE INTELLIGENCE</div><h1>{title}</h1>{subtitle&&<p>{subtitle}</p>}</div><span className="mode-pill"><i/> DEMO MODE</span></motion.div>{children}</main>}