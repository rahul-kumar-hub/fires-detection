import Page from "../components/common/Page"; import GlassCard from "../components/common/GlassCard";
const sections=[["What is NASA FIRMS?","NASA FIRMS provides satellite-based fire observations. These observations form the starting point for the intelligence pipeline."],["What is Model 2?","Model 2 converts observations into fire/activity events, builds event-level features, applies weak supervision through 17 labeling functions, and feeds weighted samples into a Random Forest."],["Why weak supervision?","The labeling functions encode contextual signals that can help generate weak labels and class probabilities without treating any single rule as the final decision."],["Why Random Forest?","The final classifier is Random Forest. It consumes the prepared training signal and produces the final five-class source prediction."],["What are the classes?","Industrial, Gas, Agriculture, Mining, and Wildfire."],["How the system works","NASA FIRMS → Events → Features → 17 LFs → Snorkel LabelModel → Weak labels + confidence → Weighted training → Random Forest → Final prediction."]]; export default function About(){return <Page title="About the Project" subtitle="The science and product story behind FIRMS Fire Intelligence."><div className="about-grid">{sections.map(s=><GlassCard key={s[0]}><div className="eyebrow">FIRE INTELLIGENCE</div><h3>{s[0]}</h3><p>{s[1]}</p></GlassCard>)}</div></Page>}