import Page from "../components/common/Page"; import GlassCard from "../components/common/GlassCard"; import {useAnalytics} from "../hooks/useAnalytics"; import {DistributionChart,TimelineChart,ConfidenceChart} from "../components/analytics/Charts";
export default function Analytics(){const {data}=useAnalytics();if(!data)return null;return <Page title="Fire Analytics" subtitle="A visual view of source distribution, confidence and temporal activity."><div className="chart-grid"><GlassCard><div className="eyebrow">FIRE SOURCE DISTRIBUTION</div><h3>Class share</h3><DistributionChart data={data}/></GlassCard><GlassCard><div className="eyebrow">EVENTS OVER TIME</div><h3>Detection activity</h3><TimelineChart data={data}/></GlassCard><GlassCard><div className="eyebrow">CONFIDENCE DISTRIBUTION</div><h3>Prediction confidence</h3><ConfidenceChart data={data}/></GlassCard><GlassCard className="trend-card"><div className="eyebrow">SOURCE TRENDS</div><h3>Demo trend view</h3><div className="trend-bars">{Object.entries(data.distribution).map(([k,v])=><div key={k}><span>{k}</span><i style={{width:`${v/1200}%`}}/><b>{v.toLocaleString()}</b></div>)}</div></GlassCard></div><div className="backend-note">DEMO ANALYTICS · <span>BACKEND TODO:</span> replace centralized demo data with aggregated API data.</div></Page>}